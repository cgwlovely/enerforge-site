# All crawlers welcome, including Googlebot and OAI-SearchBot.
# (To allow search but refuse AI training, add a disallow stanza for GPTBot —
#  deliberately not done here; the site currently allows all agents.)
User-agent: *
Allow: /

# Internal analytical artefacts. Retained in the repository as a capability but
# not published as part of the site: detailed site-specific GIS outputs, and the
# PDF reports behind the anonymised Pre-DD case, which name the asset in their
# contents. They are unlinked from every page and excluded from the sitemap.
# Patterns rather than filenames, so this file does not itself disclose what the
# anonymised case pages deliberately do not.
# Disallow is a crawling signal, not access control — see IA-NOTES.md.

Sitemap: https://heliovulcan.com.au/sitemap.xml
